# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=uptime_w33d

# Security
# Please change this to a secure random string in production
JWT_SECRET=changeme

# Frontend Configuration
# The public URL where the backend API can be accessed by the browser
# Example: http://status-api.yourdomain.com
# For local development with Docker: http://localhost:7080
VITE_API_URL=http://status-api.w33d.xyz

# Optional: Server Port (Internal Container Port, usually doesn't need changing)
# SERVER_PORT=8080
